export const ICON_WIKI_FILENAME_OVERRIDES: Record<number, string> = {
  // Permanent local overrides for icon IDs that do not match naive name->filename conversion.
  // Fill this map over time as more IDs are verified.
  // Example:
  // 45: "Conveyor_Splitter.png",
};
