
  # Modernize app UI

  This is a code bundle for Modernize app UI. The original project is available at https://www.figma.com/design/ZMN04BYM4kFdcL1S6hmWU9/Modernize-app-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  